const sortOrderList = ['asc', 'desc'];
const ACCESS_TOKEN_LIFETIME = 15 * 60 * 1000;
const REFRESH_TOKEN_LIFETIME = 7 * 24 * 3600 * 1000;

export { sortOrderList, ACCESS_TOKEN_LIFETIME, REFRESH_TOKEN_LIFETIME };
